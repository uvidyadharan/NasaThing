import skyfield.api
import math

ts = skyfield.api.load.timescale()
# t = ts.now()
# planets = load('de421.bsp')
# mars = planets['mars']
# earth = planets['earth']
# moon = planets["moon"]
# sun = planets["sun"]
#
# # From the center of the Sun (Heliocentric)
#
# earth_pos = moon.at(t).observe(earth).position.au
# sun_pos = moon.at(t).observe(sun).position.au
num = "hello world"

# earth_mag = math.sqrt(math.pow(earth_pos[0], 2) + math.pow(earth_pos[1], 2) + math.pow(earth_pos[2], 2))
